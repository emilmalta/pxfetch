# Query builders and DSL helpers.
#
# pxw_all()  — request all values for a variable
# pxw_top(n) — request the top N values (usually most recent)
# px_agg()  — aggregation helper (TBD)
#
# build_query_v1() — builds the JSON body for a v1 POST request
# build_query_v2() — builds query parameters for a v2 GET request
