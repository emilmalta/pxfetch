# Package load hook.
# .onLoad() runs when the package is attached. Keep it minimal.

.onLoad <- function(libname, pkgname) {
  # Nothing yet. Add startup checks or option defaults here as needed.
  invisible()
}
