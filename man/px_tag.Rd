% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label.R
\name{px_tag}
\alias{px_tag}
\title{Tag a tibble with PXWeb metadata}
\usage{
px_tag(data, table_id, title = NULL, .api_url = px_api_url())
}
\arguments{
\item{data}{A data frame or tibble.}

\item{table_id}{Table ID, e.g. \code{"BEXSTA"}.}

\item{title}{Table title string, typically from the API response. If \code{NULL}
(default) the \code{px_title} attribute will be \code{NA}.}

\item{.api_url}{Base URL of the API the data came from. Defaults to
\code{\link[=px_api_url]{px_api_url()}}.}
}
\value{
\code{data} with class \verb{c("px_ball", <original classes>)} and attributes
\code{px_class}, \code{px_table_id}, \code{px_title}, and \code{px_api_url}.
}
\description{
Attaches the table ID, table title, and API URL as attributes, and sets the
\code{"px_ball"} class so that pxfetch's print methods apply. \code{\link[=px_fetch]{px_fetch()}} calls
this automatically; use \code{px_tag()} directly when working with data that was
not fetched via \code{\link[=px_fetch]{px_fetch()}} but still needs \code{px_label()} support.
}
\examples{
\dontrun{
options(px.api_url = "https://bank.stat.gl/api/v1/en/Greenland/")

df <- read.csv("bexsta.csv")
df <- px_tag(df, "BEXSTA")
}
}
