% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{pxw_helpers}
\alias{pxw_helpers}
\alias{pxw_top}
\alias{pxw_all}
\alias{pxw_agg}
\alias{top}
\alias{every}
\alias{agg}
\title{Selection helpers for PXWeb queries}
\usage{
pxw_top(n = 1L)

pxw_all(pattern = "*")

pxw_agg(agg_file, ...)

top(n = 1L)

every(pattern = "*")

agg(agg_file, ...)
}
\arguments{
\item{n}{Number of values to request. For time variables this is typically
the most recent \code{n} periods.}

\item{pattern}{Wildcard pattern, e.g. \code{"*"} (all), \code{"*0"} (ending in 0),
\code{"???"} (three characters). Defaults to \code{"*"}.}

\item{agg_file}{Aggregation file name as defined in the API.}

\item{...}{Values to select from the aggregation.}
}
\value{
A modified vector with a \code{.pxw_filter} attribute consumed by the
query builders. Not intended for direct use.
}
\description{
Use these inside \code{pxw_fetch()} to control how values are selected for each
variable. Plain character vectors select specific values by code.
}
